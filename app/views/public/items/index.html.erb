<!--顧客：商品一覧-->
<div class="container px-5 px-sm-0">
  <h2>商品一覧</h2>
  <div class="row">
    
    <% @items.each do |item| %>
      <div class="col-3">
      <%= image_tag item.image %>
      <p><%= item.name %></p>
      <p><%= item.price %><span>円</span></p><br>
      </div>
    <% end %>
    
  </div>
</div>